# Linkit App Icon

`linkit-app-icon-source.png` is the generated source artwork used for:

- Android launcher mipmaps in `android/app/src/main/res/mipmap-*dpi/`
- macOS bundle icon at `macos/Resources/Linkit.icns`

Prompt:

> A premium square app icon representing fast private local file transfer between a phone and a Mac, with a clean linked-transfer symbol as the central mark. No text, no watermark, no cloud, graphite background, emerald and cyan transfer accents.
